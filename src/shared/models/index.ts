import { textEditorSlice } from "./slices";

import {
  selectTextEditorInnerHTML,
  selectTextEditorCaretPosition,
} from "./selectors";

export {
  // slices
  textEditorSlice,

  // slices

  // selectors
  selectTextEditorInnerHTML,
  selectTextEditorCaretPosition,
};
